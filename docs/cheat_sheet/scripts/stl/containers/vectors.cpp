// VECTORS:

/* 
Vectors are dynamic arrays

Support random access iterators

Supports default ctor, several parameterized ctors, itr ctors,
copy/move ctors, initializer list ctors, range-based ctors,
copy/move assignment oprs, and initializer list opr

Access -> O(1)
Insert -> O(n)
Delete -> O(n)
Search -> N/A
*/

#include <vector>

std::vector<value_type, allocator_type=> vec;